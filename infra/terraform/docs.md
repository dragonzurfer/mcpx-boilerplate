# docs.md

## Purpose

Terraform configuration for provisioning GKE, registry, and DNS resources.

## Files

- `main.tf`: primary resources (cluster, IP, registry, DNS).
- `variables.tf`: input variables.
- `outputs.tf`: output values used by deploy scripts.
- `versions.tf`: Terraform and provider constraints.
