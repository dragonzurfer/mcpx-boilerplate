# config/docs.md

## Purpose

Configuration files consumed by the backend.

## Files

- `plans.json`: manual renewal plans with `entitlementDays` windows.
  The default set is monthly + quarterly (3 months) + yearly.
